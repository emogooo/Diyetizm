<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.btdev">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BuyukFotograf"></activity>
        <activity android:name=".TarihGetir" />
        <activity android:name=".SporKayitActivity" />
        <activity android:name=".ProfilPaneli" />
        <activity android:name=".YemekKayit" />
        <activity android:name=".OgunDetay" />
        <activity android:name=".SporActivity" />
        <activity android:name=".ToplamKaloriActivity" />
        <activity android:name=".AylikKaloriActivity" />
        <activity android:name=".HaftalikKaloriActivity" />
        <activity android:name=".GunlukKaloriActivity" />
        <activity android:name=".KayitActivity" />
        <activity android:name=".GirisActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>